/**
 * Where "Report a problem" links go: the public Google Form (or support page) URL.
 * While this is null the link is hidden, and `npm run release:check` fails.
 */
export const REPORT_PROBLEM_URL: string | null = null;
